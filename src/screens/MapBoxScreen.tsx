import React from 'react';
import {Text, View} from 'react-native';
import { ParticipateMapbox } from '../components/screen_components/ParticipateMapbox';

export const MapBoxScreen = () => {

  return (
    <View style={{flex: 1}}>
      <ParticipateMapbox />
    </View>
  );
};
